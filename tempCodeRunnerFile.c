#include <stdio.h>
#include <stdlib.h>
struct node
{
    int data ;
    struct node *next;
};
void insert(struct node *ptr )
{
    while (ptr !=NULL)
    {
        printf("Enter the data : ");
        scanf("%d",&data->ptr);
        ptr = ptr->next ;
    }
}